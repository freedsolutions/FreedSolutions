# Local DB Runtime

This directory holds the local-first CRM runtime assets:

- `config.yaml` and `config.yaml.example`
- `schema.sql`
- local credentials under `credentials/` (gitignored)
- the SQLite database file (gitignored)

This directory now holds both the runtime assets and the importable Python package.
