# <TENANT> BI — Decisions record

Append-only, oldest first. One row per ruling, amendment, withdrawal,
reinstatement or measured count that used to live inside a register rule cell.
The rule itself lives in `DATA-DICTIONARY.md` §2; the full evidence lives in the
kickoff named in Record. **Never rewrite a row here — add a new one.**

| date | rule | ruling | record |
|---|---|---|---|
